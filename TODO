--Manual
+чинить состояния (просмотр детали)
выполнять procedure на стороне корлвир (мб еще и поле для БД указывать)
реализовать метод лдя прорисовпи операций (и выполенния)

удаление входящих
комбайнед имя вычислять автоматически
менб операции выислять с помощтю метода
правильно обрабатывать флаги удаление редактирование

передавать пока что XML (и еще пару полей чтобы потом актиулизирвать проще)
реализация кнопки журнал
флаг "операция отмены" на операции
в журнале метить отмененные операции цветом///


Кирилл Матвеенок, [20/10/2025 13:59]
в post() добавь к конце параметра {silent: true} чтобы не моргала

Кирилл Матвеенок, [20/10/2025 14:00]
и cache: true чтобы кэшировала вызовы, если нужно
не, useCache: true